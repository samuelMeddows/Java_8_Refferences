package com.meddows;

public class Main {

    public static void main(String[] args) {
	    // a mile is equal to 1.609344 kilometers
        double kilometers = (100 * 1.609344);

        int highScore = 50;

        if(highScore == 50) {
            System.out.println("This is an expression");
        }

        // Int the following code, write what of the code are expressions

        int score = 100;
        if (score > 99) {
            System.out.println("You got the high score!");
            score = 0;
        }

        // score = 100
        // score > 99
        // score = 0
        // NOTE: ; if () - These components are not part of the expression

    }
}

